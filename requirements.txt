# Core ML
torch>=2.0
transformers>=4.38
datasets>=2.16
accelerate>=0.26

# Data / metrics
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
tabulate>=0.9          # pandas .to_markdown() backend

# Config + tracking
PyYAML>=6.0
wandb>=0.16          # optional at runtime; pipeline falls back to disabled mode

# Optional: zero-shot Claude baseline (only needed for `cli claude-baseline`)
anthropic>=0.49

# Testing
pytest>=7.4
